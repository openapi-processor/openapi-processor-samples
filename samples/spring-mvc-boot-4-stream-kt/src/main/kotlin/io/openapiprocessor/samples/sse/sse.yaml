get:
  tags:
    - sse
  summary: stream objects
  description: endpoint has a stream response
  parameters:
    - name: source
      description: query, string
      in: query
      schema:
        type: string
  responses:
    '200':
      description: sse
      content:
        application/json:
          itemSchema:
            type: object
            properties:
              foo:
                type: string
